const Footer = () => {
    return (
        <footer className="bg-gray-900 text-white mt-20 p-5 text-center">
            <p>Movie Recommendation System</p>
            <p>Developed using React + Django + Neo4j</p>
        </footer>
    );
};

export default Footer;